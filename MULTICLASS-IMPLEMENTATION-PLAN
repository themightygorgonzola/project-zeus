
**DRAFT —** Replace the single-class character model outright, add full multiclass-aware persistence and migration, then rethread the engine, server, AI context, UI, and tests around the new shape. Based on your decisions, this plan includes Warlock `Pact Magic`, explicit per-class spell ownership, and multiclass-aware character creation now, not later. The main work is not one helper in `leveling`; it is a controlled schema migration centered on `PlayerCharacter`, spell slot ownership, subclass provenance, and per-class feature/hit-die tracking. The safest path is: redesign the model once, add versioned migration, then update each rules module against the new canonical structure before touching the creation and server surfaces.

**Steps**
1. ✅ **COMPLETE** — Redesign `PlayerCharacter` in types.ts from single-class fields to explicit multiclass structures:
   - Removed `class`, `subclass`, `spellcastingAbility`, `hitDiceRemaining`, `knownSpells`, `preparedSpells`, `cantrips`.
   - Added `classes: ClassLevel[]` (canonical per-class level, subclass, hitDiceRemaining), `classSpells: ClassSpellList[]` (per-class cantrips/known/prepared), `pactSlots: SpellSlotPool[]` (Warlock Pact Magic).
   - Added `sourceClass?: ClassName` on `CharacterFeatureRef` for feature provenance.
   - Added 12 pure helper functions: `getPrimaryClass`, `getPrimarySubclass`, `hasClass`, `getClassEntry`, `getClassLevelNum`, `getAllKnownSpells`, `getAllPreparedSpells`, `getAllCantrips`, `getPrimarySpellcastingAbility`, `getTotalHitDiceRemaining`, `getHitDiceForClass`, `getClassSpellEntry`.
   - `level` kept as convenience total (sum of all class levels).
   - Added `importClasses?: ClassLevel[]` to `CharacterCreateInput` for multiclass creation.
   - `GAME_STATE_VERSION` bumped from 2 to 3.

2. ✅ **COMPLETE** — Bump state version and update normalization in types.ts and state.ts:
   - `GAME_STATE_VERSION = 3`.
   - `normalizeCharacter()` normalizes `classes: ClassLevel[]`, `classSpells: ClassSpellList[]`, `pactSlots: SpellSlotPool[]`.
   - Computes `level = sum(classes[].level)`.
   - Ensures at least one class entry (defaults to fighter level 1).
   - Legacy single-class save support is now covered in `state.ts`: old `class`/`subclass`/`spellcastingAbility`/`cantrips`/`knownSpells`/`preparedSpells`/`hitDiceRemaining` fields normalize into `classes[]` and `classSpells[]`; legacy Warlock `spellSlots` migrate into `pactSlots`.

3. ✅ **COMPLETE** — Make class/rules data the single source of multiclass truth in classes.ts:
   - Exported `FULL_CASTER_SLOTS` and `PACT_SLOTS` tables.
   - Added `getMulticlassSpellSlots(classes: ClassLevel[]): number[]` — computes effective caster level from full/half/third contributions per 5e PHB rules.
   - Added `getPactSlotInfo(warlockLevel: number): { count: number; slotLevel: number } | null` — parses PACT_SLOTS for Warlock Pact Magic.
   - All consumer modules updated: leveling.ts, spellcasting.ts, rest.ts, inventory.ts, gm-context.ts, character-creation.ts.
   - All 8 existing test files updated, plus new multiclass.test.ts (40 tests).
   - **783/783 tests passing, 0 TypeScript errors.**

4. ✅ **COMPLETE** — Convert `leveling` from "one class levels up" to "one class entry levels up" in leveling.ts:
   - `LevelUpChoices` now has `targetClass?: ClassName` field (defaults to primary class).
   - `applyLevelUp()` fully rewritten: detects new-class vs existing-class, enforces `checkMulticlassPrereqs()` for both leaving and entering classes, applies `multiclassGrants` (armor/weapon/tool proficiencies), creates new `ClassLevel` + `ClassSpellList` entries.
   - ASI detection uses `isASILevelForClass(targetClassName, newClassLevel)` — CLASS level, not total level.
   - HP uses target class's hit die. Features come from target class at target CLASS level with `sourceClass` tag.
   - Spell slots: if multiple non-pact caster classes → `getMulticlassSpellSlots()`; otherwise single-class table.
   - Pact slot initialization for new warlocks and progression for existing warlocks.
   - `recalculateDerivedStats()` computes `maxPreparedSpells` per-class using class level and class-specific casting ability.
   - 18 new multiclass leveling tests added.

5. ✅ **COMPLETE** — Rebuild spellcasting around per-class ownership in spellcasting.ts:
   - Added `getSpellSaveDCForClass(character, className)` and `getSpellAttackBonusForClass(character, className)` for per-class DC/attack bonus.
   - `canCastSpell()` now checks both standard slots AND pact slots — prefers standard, falls back to pact via `findAvailablePactSlot()`.
   - `expendSpellSlot()` tries standard slots first, then pact slots; returns `{ success, remaining, reason?, pactSlotUsed?: boolean }`.
   - Added `findAvailablePactSlot(character, minLevel)` helper for pact slot discovery.
   - New exports added to `index.ts`: `getSpellSaveDCForClass`, `getSpellAttackBonusForClass`.
   - 10 new multiclass spellcasting tests added (per-class DC, pact slot integration, slot preference).

6. ✅ **COMPLETE** — Rebuild hit-die and rest recovery around per-class pools in rest.ts:
   - `ShortRestPreview` enhanced with `perClassDice: { className, dieSides, available, total }[]` and `pactSlotsToRecover: number`.
   - `previewShortRest()` computes per-class die breakdown and pact slot recovery count.
   - `shortRest()` and `longRest()` were already multiclass-aware from steps 1-3 (per-class hit dice spending/recovery, pact slot recovery on short rest).
   - 7 new multiclass rest tests added (per-class dice preview, pact slot recovery, long rest proportional hit die restore).
   - **815/815 tests passing, 0 TypeScript errors.**

7. ✅ **COMPLETE** — Update creation flow to support multiclass-aware data now in character-creation.ts, [site/src/routes/api/adventure/[id]/character/+server.ts](site/src/routes/api/adventure/[id]/character/+server.ts), and CharacterCreation.svelte:
   - `buildFeatureList()` now tags all class/subclass features with `sourceClass: input.class`.
   - Added `buildImportedClassFeatures()` — builds features from each imported class at their class level with correct `sourceClass`.
   - Added `buildImportedClassSpells()` — builds `ClassSpellList` entries per caster class for multiclass imports; primary class gets input spells, others start empty.
   - `createCharacter()` merges imported features and spell entries; `featureUses` built from merged list at total level.
   - Character route and CharacterCreation.svelte unchanged — they correctly delegate to game layer.
   - 20 new tests in character-creation.test.ts (single-class sourceClass, imported multiclass features/spells, `buildFeatureUses`, validation).
   - **845/845 tests passing, 0 TypeScript errors.**

8. ✅ **COMPLETE** — Update display and derived consumers that currently assume one class in CharacterSheet.svelte, gm-context.ts, and index.ts:
   - CharacterSheet.svelte: Replaced 6 broken legacy field references (`character.class`, `character.subclass`, `character.hitDiceRemaining`, `character.cantrips`, `character.knownSpells`, `character.preparedSpells`) with multiclass-aware helpers (`getAllCantrips`, `getAllKnownSpells`, `getAllPreparedSpells`, `getTotalHitDiceRemaining`).
   - CharacterSheet.svelte: Class display now renders multiclass stacks like "Fighter 5 / Wizard 3".
   - CharacterSheet.svelte: Added pact slot display section.
   - gm-context.ts `formatCharacterBrief()`: Enhanced with per-class spellcasting summary (DC, attack bonus, cantrip/prepared counts per class), slot summary, and pact slot summary.
   - index.ts: No changes needed — all helpers already exported from previous steps.
   - 10 new tests in gm-context.test.ts (character brief rendering, spell info, pact slots, message structure, history, quests, mechanics).
   - **845/845 tests passing, 0 TypeScript errors.**

9. ✅ **COMPLETE** — Make AI/server turn logic multiclass-aware in adventure-turn.ts and [site/src/routes/api/adventure/[id]/turn/+server.ts](site/src/routes/api/adventure/[id]/turn/+server.ts):
   - adventure-turn.ts: No changes needed — delegates entirely to `assembleGMContext()` which is now multiclass-aware (step 8 enhanced `formatCharacterBrief`).
   - Turn server route: No changes needed — validates membership and dispatches; no direct character model access.
   - Verified zero legacy field references (`character.class`, `cantrips`, `knownSpells`, `preparedSpells`, `hitDiceRemaining`) in either file.
   - GM now receives per-class DC/attack bonus, slot pools, and pact slot info in every turn's system prompt.
   - **845/845 tests passing, 0 TypeScript errors.**

10. ✅ **COMPLETE** — Patch secondary rule consumers that use class or level shortcuts:
   - inventory.ts: Already multiclass-aware — `computeAC()` uses `hasClass(character, 'barbarian')` and `hasClass(character, 'monk')` for unarmored defense. No changes needed.
   - travel.ts: Already correct — `getAveragePartyLevel()` uses `c.level` (total level). No changes needed.
   - Full codebase sweep confirmed zero remaining references to removed `PlayerCharacter` fields (`class`, `cantrips`, `knownSpells`, `preparedSpells`, `hitDiceRemaining`, `spellcastingAbility`) outside of test fixtures and the new multiclass model.
   - **845/845 tests passing, 0 TypeScript errors.**

11. ✅ **COMPLETE** — Expand and migrate the test suite across existing and new files:
   - Added `state.test.ts` (5 tests) covering `migrateState()` for invalid input, existing v3 multiclass state, legacy single-class wizard migration, legacy Warlock pact-slot migration, and old lobby-created state normalization.
   - Added `character-server.test.ts` (3 tests) for server-facing character GET/POST handling: current-user lookup, validation failure payload, and successful character persistence.
   - Added `adventure-turn.test.ts` (4 tests) for turn payload assembly and profile resolution: structured history shape, fallback prompt, inline/background profile behavior.
   - Expanded existing suites with dedicated multiclass scenarios:
     - `leveling.test.ts`: `Paladin 2 / Sorcerer 3` effective caster level.
     - `rest.test.ts`: `Rogue 2 / Wizard 3` per-class hit-die preview.
     - `spellcasting.test.ts`: `Warlock 3 / Bard 3` separate pact vs standard slot pools.
     - Existing coverage already included `Fighter/Wizard`, `Cleric/Wizard`, and `Warlock` + another caster scenarios.
   - Full suite now: **860/860 tests passing**.

12. ✅ **COMPLETE** — Update the planning/status document in CONTENT-COMPLETE-PUSH-PHASES after implementation, including the new schema version, migration note, and total test count:
   - Added a multiclass completion addendum to `CONTENT-COMPLETE-PUSH-PHASES`.
   - Documented schema version `3`, legacy single-class migration support, Warlock pact-slot migration behavior, and final verification counts.

**Verification**
- Typecheck the whole site with `npm run check`.
- Run the full test suite with `npx vitest run`.
- Add explicit migration verification:
  - legacy single-class save loads into the new multiclass model,
  - old lobby-created state still normalizes correctly,
  - no existing single-class characters regress.
- Add core multiclass rule scenarios:
  - `Wizard 3 / Cleric 2` shared slot calculation and per-class prepared spell checks,
  - `Paladin 2 / Sorcerer 3` effective caster level and spell slot usage,
  - `Warlock 3 / Bard 3` separate `Pact Magic` vs standard slots,
  - `Fighter 2 / Wizard 3` per-class hit dice spend on short rest,
  - gaining a second class through `applyLevelUp()` enforces `checkMulticlassPrereqs()`,
  - AI/GM context renders a multiclass sheet correctly and turn routes persist it unchanged.

**Decisions**
- Replace the single-class model now rather than keeping `class` as a migration shim.
- Include full Warlock `Pact Magic` support in the first implementation.
- Track spells with explicit per-class source ownership.
- Support multiclass in creation/import paths now, not only post-creation leveling.
- Treat the game layer as authoritative; server and AI context must be updated to consume the same canonical multiclass model.